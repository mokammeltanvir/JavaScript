/* Library Functions of Javascript*/

// 1. Get the element by id
function $(id) {
    return document.getElementById(id);
}

// 2. Get the element by tag name
function $$(tagName) {
    return document.getElementsByTagName(tagName);
}

// 3. Get the element by class name
function $$$$(className) {
    return document.getElementsByClassName(className);
}

// 4. Get the element by name
function $$$$ (name) {
    return document.getElementsByName(name);
}

// 5. Get the element by query selector
function $$$$$ (selector) {
    return document.querySelector(selector);
}

// 6. Get the element by query selector all
function $$$$$$ (selector) {
    return document.querySelectorAll(selector);
}

// 7. Get the element by query selector all
function $$$$$$$ (selector) {
    return document.querySelectorAll(selector);
}

// 8. Get the element by query selector all
function $$$$$$$$ (selector) {
    return document.querySelectorAll(selector);
}

// 9. Get the element by query selector all
function $$$$$$$$$ (selector) {
    return document.querySelectorAll(selector);
}

